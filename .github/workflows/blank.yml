name: Mixed inputs

on:
  workflow_dispatch:
    inputs:
      message:
        required: true
      
jobs:
  greet:
    runs-on: ubuntu-latest

    steps:
    - name: Send greeting
      run: echo "${{ github.event.inputs.message }}"