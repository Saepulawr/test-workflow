name: Mixed inputs

on:
  workflow_dispatch:

  # Allows external webhook trigger
  repository_dispatch:
    types:
      - webhook
    inputs:
      name:

jobs:
  greet:
    runs-on: ubuntu-latest

    steps:
    - name: Send greeting
      run: 'echo "payload: ${{ github.event.client_payload }}"'